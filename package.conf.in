#include "HsRegexPosixConfig.h"

name:		PACKAGE
version:	VERSION
license:	BSD3
maintainer:	MAINTAINER
exposed:	True

exposed-modules:
	Text.Regex.Posix
	Text.Regex.Posix.String
	Text.Regex.Posix.ByteString
	Text.Regex.Posix.Wrap

hidden-modules:

import-dirs:	IMPORT_DIR

library-dirs:	LIB_DIR
#if !defined(INSTALLING) && !HAVE_POSIX_REGEX
		, LIB_DIR"/cbits"
#endif

hs-libraries:   "HSregex-posix"

#if !HAVE_POSIX_REGEX
extra-libraries: HSregex_posix_cbits
#endif

include-dirs: 	INCLUDE_DIR
includes:
depends:	base regex-base

haddock-interfaces:	HADDOCK_IFACE
haddock-html:		HTML_DIR
